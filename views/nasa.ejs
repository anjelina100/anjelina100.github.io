<!DOCTYPE html>
<html lang="en">

<head>
    <%-include("partials/head.ejs")%>
</head>

<body>
    <h1 class="bg-info text-white p-3 m-0"> The Solar System </h1>

    <%- include("partials/nav.ejs")%>
        <div class="col card">
            <% if (nasaInfo.media_type==='image' ) { %>
                <img src="<%= nasaInfo.url %>">
                <% } else { %>
                    <video src="<%= nasaInfo.url %>" controls></video>
                    <% } %>
        </div>

        <%-include("partials/footer.ejs")%>
</body>

</html>